// Package transport is the WhatsApp transport over whatsmeow: connect, session
// DB, reconnect/logout handling, and translating whatsmeow events into the
// normalized InboundMessage envelope.
//
// Not yet implemented — see ../SPEC.md sections 3, 4, 12. Lands in Phase 1
// (based on amit/whatsapp-mgr's internal/wa.Client).
package transport
